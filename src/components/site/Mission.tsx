export function Mission() {
  return (
    <section className="bg-foreground py-28 text-background md:py-40">
      <div className="container-x">
        <p className="text-xs font-semibold uppercase tracking-[0.14em] text-background/60">
          / Our Mission
        </p>
        <p className="mt-10 max-w-5xl text-[clamp(2rem,4.5vw,4.25rem)] font-bold leading-[1.05] tracking-tight">
          To move our clients' business forward with reliable, transparent and
          intelligent shipping — connecting India to every market that matters,
          one cargo at a time.
        </p>
      </div>
    </section>
  );
}
